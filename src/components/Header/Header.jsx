import React from 'react';
import Nav from '../Nav/Nav';
import './Header.css';

function Header() {
	return (
		<header>
			<p id="logo">Kayle</p>
			<Nav />
		</header>
	);
}

export default Header;
